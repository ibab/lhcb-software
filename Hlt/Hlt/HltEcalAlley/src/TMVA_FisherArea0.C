// Class: ReadFisherArea0
// Automatically generated by MethodBase::MakeClass
//

/* configuration options =====================================================

#GEN -*-*-*-*-*-*-*-*-*-*-*- general info -*-*-*-*-*-*-*-*-*-*-*-

Method         : Fisher::Fisher
TMVA Release   : 3.8.14        [198670]
ROOT Release   : 5.13/06       [331014]
Creator        : witekma
Date           : Sat Jul 12 20:21:21 2008
Host           : Linux lxplus071.cern.ch 2.6.9-42.0.3.EL.cernsmp #1 SMP Fri Oct 6 12:07:54 CEST 2006 i686 i686 i386 GNU/Linux
Dir            : /home/witekma/root/ecal_TMVA/TMVA/macros
Training events: 400


#OPT -*-*-*-*-*-*-*-*-*-*-*-*- options -*-*-*-*-*-*-*-*-*-*-*-*-

# Set by User:
Normalise: "False" [Normalise input variables]
NbinsMVAPdf: "50" [Number of bins used to create MVA PDF]
NsmoothMVAPdf: "1" [Number of smoothing iterations for MVA PDF]
V: "False" [Verbose mode]
H: "True" [Print classifier-specific help message]
CreateMVAPdfs: "True" [Create PDFs for classifier outputs]
Method: "Fisher" [Discrimination method]
# Default:
D: "False" [use-decorrelated-variables flag (depreciated)]
VarTransform: "None" [Variable transformation method]
VarTransformType: "Signal" [Use signal or background events for var transform]
VerboseLevel: "Info" [Verbosity level]
TxtWeightFilesOnly: "True" [if True, write all weights as text files]
##


#VAR -*-*-*-*-*-*-*-*-*-*-*-* variables *-*-*-*-*-*-*-*-*-*-*-*-

NVar 5
showershape                   showershape                       'F'    [0.130854204297,0.99998998642]
showerasym                    showerasym                        'F'    [0.00167112413328,0.873481690884]
showerkappa                   showerkappa                       'F'    [0.00567073328421,0.87805134058]
showertail                    showertail                        'F'    [0.156523570418,0.966446995735]
showerenergy                  showerenergy                      'F'    [0.018981071189,0.57693529129]


============================================================================ */

#include <vector>
#include <cmath>
#include <string>
#include <iostream>

#ifndef IClassifierReader__def
#define IClassifierReader__def

class IClassifierReader {

 public:

   // constructor
   IClassifierReader() {}
   virtual ~IClassifierReader() {}

   // return classifier response
   virtual double GetMvaValue( const std::vector<double>& inputValues ) const = 0;
};

#endif

class ReadFisherArea0 : public IClassifierReader {

 public:

   // constructor
   ReadFisherArea0( std::vector<std::string>& theInputVars ) 
      : IClassifierReader(),
        fClassName( "ReadFisherArea0" ),
        fStatusIsClean( true ),
        fNvars( 5 ),
        fIsNormalised( false )
   {      
      // the training input variables
      const char* inputVars[] = { "showershape", "showerasym", "showerkappa", "showertail", "showerenergy" };

      // sanity checks
      if (theInputVars.size() <= 0) {
         std::cout << "Problem in class \"" << fClassName << "\": empty input vector" << std::endl;
         fStatusIsClean = false;
      }

      if (theInputVars.size() != fNvars) {
         std::cout << "Problem in class \"" << fClassName << "\": mismatch in number of input values: "
                   << theInputVars.size() << " != " << fNvars << std::endl;
         fStatusIsClean = false;
      }

      // validate input variables
      for (size_t ivar = 0; ivar < theInputVars.size(); ivar++) {
         if (theInputVars[ivar] != inputVars[ivar]) {
            std::cout << "Problem in class \"" << fClassName << "\": mismatch in input variable names" << std::endl
                      << " for variable [" << ivar << "]: " << theInputVars[ivar].c_str() << " != " << inputVars[ivar] << std::endl;
            fStatusIsClean = false;
         }
      }

      // initialize min and max vectors (for normalisation)
      fVmin[0] = 0.130854204297066;
      fVmax[0] = 0.999989986419678;
      fVmin[1] = 0.00167112413328141;
      fVmax[1] = 0.873481690883636;
      fVmin[2] = 0.0056707332842052;
      fVmax[2] = 0.878051340579987;
      fVmin[3] = 0.156523570418358;
      fVmax[3] = 0.966446995735168;
      fVmin[4] = 0.0189810711890459;
      fVmax[4] = 0.576935291290283;

      // initialize input variable types
      fType[0] = 'F';
      fType[1] = 'F';
      fType[2] = 'F';
      fType[3] = 'F';
      fType[4] = 'F';

      // initialize constants
      Initialize();

   }

   // destructor
   virtual ~ReadFisherArea0() {
      Clear(); // method-specific
   }

   // the classifier response
   // "inputValues" is a vector of input values in the same order as the 
   // variables given to the constructor
   double GetMvaValue( const std::vector<double>& inputValues ) const;

 private:

   // method-specific destructor
   void Clear();

   // common member variables
   const char* fClassName;
   bool fStatusIsClean;

   const size_t fNvars;
   size_t GetNvar()           const { return fNvars; }
   char   GetType( int ivar ) const { return fType[ivar]; }

   // normalisation of input variables
   const bool fIsNormalised;
   bool IsNormalised() const { return fIsNormalised; }
   double fVmin[5];
   double fVmax[5];
   double NormVariable( double x, double xmin, double xmax ) const {
      // normalise to output range: [-1, 1]
      return 2*(x - xmin)/(xmax - xmin) - 1.0;
   }

   // type of input variable: 'F' or 'I'
   char   fType[5];

   // initialize internal variables
   void Initialize();
   double GetMvaValue__( const std::vector<double>& inputValues ) const;

   // private members (method specific)
   double              fFisher0;
   std::vector<double> fFisherCoefficients;
};

inline void ReadFisherArea0::Initialize() 
{
   fFisher0 = 0.809678661977;
   fFisherCoefficients.push_back( -1.27841359156 );
   fFisherCoefficients.push_back( -0.478654250289 );
   fFisherCoefficients.push_back( -0.140107266017 );
   fFisherCoefficients.push_back( -0.00343305184596 );
   fFisherCoefficients.push_back( 0.732739943532 );

   // sanity check
   if (fFisherCoefficients.size() != fNvars) {
      std::cout << "Problem in class \"" << fClassName << "\"::Initialize: mismatch in number of input values"
                << fFisherCoefficients.size() << " != " << fNvars << std::endl;
      fStatusIsClean = false;
   }         
}

inline double ReadFisherArea0::GetMvaValue__( const std::vector<double>& inputValues ) const
{
   double retval = fFisher0;
   for (size_t ivar = 0; ivar < fNvars; ivar++) {
      retval += fFisherCoefficients[ivar]*inputValues[ivar];
   }

   return retval;
}

// Clean up
inline void ReadFisherArea0::Clear() 
{
   // clear coefficients
   fFisherCoefficients.clear(); 
}
inline double ReadFisherArea0::GetMvaValue( const std::vector<double>& inputValues ) const
{
   // classifier response value
   double retval = 0;

   // classifier response, sanity check first
   if (!fStatusIsClean) {
      std::cout << "Problem in class \"" << fClassName << "\": cannot return classifier response"
                   << " because status is dirty" << std::endl;
      retval = 0;
   }
   else {
      if (IsNormalised()) {
         // normalise variables
         std::vector<double> iV;
         int ivar = 0;
         for (std::vector<double>::const_iterator varIt = inputValues.begin();
              varIt != inputValues.end(); varIt++, ivar++) {
            iV.push_back(NormVariable( *varIt, fVmin[ivar], fVmax[ivar] ));
         }
         retval = GetMvaValue__( iV );
      }
      else {
         retval = GetMvaValue__( inputValues );
      }
   }

   return retval;
}
