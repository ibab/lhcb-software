#============================================================================
# Created     : 2006-09-31
# Maintainers : Johan Blouw, Physikalisches Institut, Heidelberg
#		Louis Nicolas, EPFL, Lausanne
#		Adlene Hicheur, EPFL, Lausanne
#============================================================================
package           AlignTrTools
version           v3r0

#============================================================================
# Structure, i.e. directories to process. 
#============================================================================
branches          AlignTrTools cmt doc src
include_dirs	  $(ALIGNTRTOOLSROOT)

#============================================================================
# Used packages. Specify the version, * at the end specifies 'any revision'
#    Put as many lines as needed, with all packages, without the '#'
#============================================================================
use cernlib v* LCG_Interfaces
use   GSL             v*     LCG_Interfaces

use   GaudiAlg        v*
use   LHCbKernel      v*     Kernel
use   LHCbMath        v*     Kernel
use   Relations       v*     Kernel

use AlignmentInterfaces v2r0 Alignment
use AlignmentTools    v2r5   Alignment

use   MCEvent         v*     Event
use   RecEvent        v*     Event
use   TrackEvent      v*     Event
use   DigiEvent       v*     Event
use   LinkerEvent     v*     Event

use   TrackInterfaces v*     Tr
use   TrackFitEvent   v*     Tr
use   TsaKernel       v*     Tr

use   STDet           v*     Det
use   OTDet           v*     Det
use   VeloDet         v*     Det

use HbookCnv v*

macro cernlib_linkopts " -L$(cernlib_home)/lib  -llapack3 -lblas -lpacklib -lphtools -lkernlib"
#============================================================================
# Component library building rule
#============================================================================
library          AlignTrTools    ../src/*.cpp ../src/minres/*.f -import=AIDA
#============================================================================
# define component library link options
#============================================================================
apply_pattern    component_library library=AlignTrTools
set ALIGNTRTOOLSOPTS  ${ALIGNTRTOOLSROOT}/options
