target_directory = ${CMTINSTALLAREA}/TCK
MANIFEST_VERSION = ${MOOREVERSION}

manifest ::  
	mkdir -p $(target_directory)
	test -f ${TCKDATAROOT}/manifest/MOORE_$(MANIFEST_VERSION) && cp ${TCKDATAROOT}/manifest/MOORE_$(MANIFEST_VERSION) $(target_directory)/manifest; true
