//---------------------------------------------------------------------------
// Job options to dump the HepMC record written by a previous test
//---------------------------------------------------------------------------
ApplicationMgr.HistogramPersistency = "NONE";
ApplicationMgr.EvtMax   = 2;
EventSelector.PrintFreq = 1;

// Set up POOL persistency
#include "$GAUDIPOOLDBROOT/options/GaudiPoolDbRoot.opts"

// Select an input file type
EventSelector.Input = {"DATAFILE='PFN:PoolDst.root' TYP='POOL_ROOTTREE' OPT='READ'"};

// Run the algorithm
ApplicationMgr.TopAlg += { "DumpHepMC" };
