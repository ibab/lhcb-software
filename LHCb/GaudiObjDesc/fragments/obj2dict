$(dest)${NAME}.obj2dict :  ${FULLNAME}
	@echo Producing Dictionary from ${FULLNAME}
	@if !(test -d $(dest)); then mkdir $(dest); fi 
	@cd $(dest); $(GAUDIOBJDESCROOT)/$(tag)/$(parsetool) $(GODflags) ${FULLNAME}
	@echo /dev/null > $@