$(dest)${NAME}.obj2doth : ${FULLNAME} 
	@echo Producing Header files from ${FULLNAME}
	@if !(test -d $(dest)); then mkdir $(dest); fi 
	@cd $(dest); $(GAUDIOBJDESCROOT)/$(tag)/$(parsetool) $(GODflags) $(${CONSTITUENT}_GODflags) -x $(GAUDIOBJDESCROOT)/xml_files/ ${FULLNAME}
	@echo /dev/null > $@
