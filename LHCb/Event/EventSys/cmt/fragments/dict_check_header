${CONSTITUENT} ::
	@if $(doCheck); then \
          source setup.sh; lcgdictcheck --cmt --info -s $(GAUDIPOOLDBROOT)/$(BINDIR)/libGaudiPoolDb.so libSealSTLDict.so; \
        else \
          echo "Not doing LCG dict check"; \
        fi;

