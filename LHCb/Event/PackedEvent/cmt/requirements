#============================================================================
# Created    : 2005-02-07
# Maintainer : Olivier Callot, Chris Jones
#============================================================================
package           PackedEvent
version           v4r13

#============================================================================
# Structure, i.e. directories to process.
#============================================================================
branches          cmt doc Event dict src

#============================================================================
# Used packages. Specify the version, * at the end specifies 'any revision'
#    Put as many lines as needed, with all packages, without the '#'
#============================================================================
use	GaudiKernel     v*
use	GaudiAlg        v*
use     LHCbKernel      v*     Kernel
use     MCEvent         v*     Event
use     RecEvent        v*     Event
use     PhysEvent       v*     Event

apply_pattern install_more_includes more=Event

# ================ linker library ============================================
library PackedEventLib ../src/*.cpp
apply_pattern linker_library library=PackedEventLib

#============================================================================
# Produce lcg dictionary
# for stl containers used in PackedEvent
#============================================================================
apply_pattern reflex_dictionary \
                  dictionary=PackedEvent \
                  headerfiles=$(PACKEDEVENTROOT)/dict/PackedEventDict.h     \
                  selectionfile=$(PACKEDEVENTROOT)/dict/PackedEventDict.xml

# This package has been disabled (all the platforms we build are 'Unix')
apply_pattern disable_package_on platform=Unix