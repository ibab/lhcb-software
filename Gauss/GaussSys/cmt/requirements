package GaussSys
version v35r0

branches cmt doc

#==================================================================
# Gauss packages
#==================================================================

# Gauss application: the application is not built on Win32
macro Gauss_use         "Gauss        v35r0   Sim"  WIN32 ""
use ${Gauss_use} 

# Generator packages: only a fraction available for release on Win32
# but temporarly switch them off on Win32
macro GENSER_use        "GENSER       v4r2    Gen"  WIN32 ""
macro EvtGen_use        "EvtGen       v9r2    Gen"  WIN32 ""
macro Generators_use    "Generators   v9r0    Gen"  WIN32 ""
macro LbPythia_use      "LbPythia     v8r0    Gen"  WIN32 ""
use ${GENSER_use}
use ${EvtGen_use}
use ${Generators_use}
use ${LbPythia_use}
#
#
# TEMPORARLY switch off all generators extensions to Pythia6
macro LbAlpGen_use      "LbAlpGen     v1r0    Gen"  WIN32 ""
macro BcVegPy_use       "BcVegPy      v1r1p2  Gen"  WIN32 ""
macro LbBcVegPy_use     "LbBcVegPy    v2r0    Gen"  WIN32 ""
macro LbHerwig_use      "LbHerwig     v6r0    Gen"  WIN32 ""     
macro LbHidValley_use   "LbHidValley  v2r2    Gen"  WIN32 ""
macro LbHijing_use      "LbHijing     v4r0    Gen"  WIN32 ""
macro LbPythia8_use     "LbPythia8    v2r0    Gen"  WIN32 ""
#use ${LbAlpGen_use}
use ${BcVegPy_use}
use ${LbBcVegPy_use}
use ${LbHerwig_use}
#use ${LbHidValley_use}
use ${LbHijing_use}
use ${LbPythia8_use}
#
use LbMIB               v3r0      Gen
# temporarly switch it off on Win32
macro ParticleGuns_use  "ParticleGuns v6r0    Gen"  WIN32 ""
use ${ParticleGuns_use}

# Simulation basic packages
use GiGa        	v19r2	  Sim     
use GiGaCnv     	v18r5	  Sim
use SimSvc              v5r1      Sim
macro GiGaVisUI_use     "GiGaVisUI    v5r0    Sim"  WIN32 ""
use ${GiGaVisUI_use} 
use VisSvc              v5r3      Vis 
use ClhepTools          v2r1      Tools

# Simulation specialized packages
use GaussKine           v3r0      Sim
use GaussAlgs		v3r3      Sim
use GaussTools          v15r0     Sim
use GaussRICH		v9r1	  Sim
use GaussTracker	v6r1	  Sim
use GaussCalo		v9r2	  Sim
use GaussPhysics	v6r0	  Sim

# Monitoring packages
use GaussMonitor	v6r0	  Sim
use CaloMoniSim         v3r1p1    Calo
use VeloGauss           v2r3      Velo
use MuonMoniSim         v2r3      Muon
use BcmMoniSim          v1r1      Bcm

# GMDL
#use GDML                v1r240p0  Sim
#use GDMLG4Writer        v2r0      Sim

# Visualization configuration
use XmlVis              v1r7      Vis

# Test packages for new releases or installations on new platforms
use GenTests            v1r3      Gen

# Allow the generation of QMTest summary 
apply_pattern QMTestSummarize

