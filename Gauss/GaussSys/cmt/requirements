package GaussSys
version v37r0

branches cmt doc

#==================================================================
# Gauss packages
#==================================================================

# Gauss application: the application is not built on Win32
macro Gauss_use         "Gauss        v37r0   Sim"  WIN32 ""
use ${Gauss_use} 

# Generator packages: Basic configuration
macro GENSER_use        "GENSER       v5r1    Gen"  WIN32 ""
macro EvtGen_use        "EvtGen       v11r0   Gen"  WIN32 ""
macro EvtGenExtras_use  "EvtGenExtras v2r0    Gen"  WIN32 ""
macro Generators_use    "Generators   v10r0    Gen"  WIN32 ""
macro LbPythia_use      "LbPythia     v8r3    Gen"  WIN32 ""
use ${GENSER_use}
use ${EvtGen_use}
use ${EvtGenExtras_use}
use ${Generators_use}
use ${LbPythia_use}

# Generators packages: extensions to Pythia6
macro LbAlpGen_use      "LbAlpGen     v1r0    Gen"  WIN32 ""
macro BcVegPy_use       "BcVegPy      v1r1p3  Gen"  WIN32 ""
macro LbBcVegPy_use     "LbBcVegPy    v2r0    Gen"  WIN32 ""
macro LbHidValley_use   "LbHidValley  v2r2    Gen"  WIN32 ""
#use ${LbAlpGen_use}
use ${BcVegPy_use}
use ${LbBcVegPy_use}
#use ${LbHidValley_use}

# Generators packages: alternative to Pythia6 for pp collisions
macro LbHerwig_use      "LbHerwig     v6r0p1  Gen"  WIN32 ""
macro LbPythia8_use     "LbPythia8    v2r0    Gen"  WIN32 ""
use ${LbHerwig_use}
use ${LbPythia8_use}

# Generators packages: alternative to Pythia6 for p-A collisions
macro LbHijing_use      "LbHijing     v5r0    Gen"  WIN32 ""
use ${LbHijing_use}

# Generators packages: LHCb sampling results for Machine Induced Background
use LbMIB               v3r2      Gen

# Generators packages: LHCb particle gun generator: temporarly off on Win32
macro ParticleGuns_use  "ParticleGuns v7r0    Gen"  WIN32 ""
use ${ParticleGuns_use}

# Simulation basic packages
use GiGa        	v19r4	  Sim     
use GiGaCnv     	v18r6	  Sim
use SimSvc              v5r1      Sim
macro GiGaVisUI_use     "GiGaVisUI    v5r1    Sim"  WIN32 ""
use ${GiGaVisUI_use} 
use VisSvc              v5r3      Vis 
use ClhepTools          v2r1      Tools

# Simulation specialized packages
use GaussKine           v3r4      Sim
use GaussAlgs		v3r4      Sim
use GaussTools          v15r2     Sim
use GaussRICH		v9r4	  Sim
use GaussTracker	v6r2	  Sim
use GaussCalo		v9r3	  Sim
use GaussPhysics	v6r2	  Sim

# Monitoring packages
use GaussMonitor	v6r3      Sim
use CaloMoniSim         v3r2      Calo
use VeloGauss           v2r4      Velo
use MuonMoniSim         v2r4      Muon
use BcmMoniSim          v1r2      Bcm

# GMDL
#use GDML                v1r240p0  Sim
#use GDMLG4Writer        v2r0      Sim

# Visualization configuration
use XmlVis              v1r11      Vis

# Test packages for new releases or installations on new platforms
use GenTests            v1r3      Gen
use SimChecks           v1r0      Sim

# Allow to run the QMTests  
apply_pattern QMTest

# Allow the generation of QMTest summary 
apply_pattern QMTestSummarize

