package GaussSys
version v30r5

branches cmt doc

#==================================================================
# Gauss packages
#==================================================================

# Gauss application: the application is not built on Win32
macro Gauss_use       "Gauss        v30r5   Sim"  WIN32 ""
use ${Gauss_use} 

# Generator packages: only a fraction available for release on Win32
use LHCbDefinitions     v2r0      Kernel
use GENSER              v3r3      Gen
use EvtGen              v8r17     Gen   
use Generators          v7r6      Gen
use LbPythia            v5r4      Gen
#
macro LbPythia8_use   "LbPythia8    v1r1    Gen"  WIN32 ""
macro LbAlpGen_use    "LbAlpGen     v1r0    Gen"  WIN32 ""
macro BcVegPy_use     "BcVegPy      v1r1p2  Gen"  WIN32 ""
macro LbBcVegPy_use   "LbBcVegPy    v1r5    Gen"  WIN32 ""
macro LbHerwig_use    "LbHerwig     v5r2    Gen"  WIN32 ""     
macro LbHidValley_use "LbHidValley  v2r0    Gen"  WIN32 ""
macro LbHijing_use    "LbHijing     v3r1    Gen"  WIN32 ""
use ${LbPythia8_use}
use ${LbAlpGen_use}
use ${BcVegPy_use}
use ${LbBcVegPy_use}
use ${LbHerwig_use}
use ${LbHidValley_use}
use ${LbHijing_use}
#
use LbMIB               v2r1      Gen
use ParticleGuns	v3r4      Gen

# Simulation basic packages
use GiGa        	v19r1	  Sim     
use GiGaCnv     	v18r4	  Sim
use SimSvc              v5r0      Sim
macro GiGaVisUI_use   "GiGaVisUI    v4r0    Sim"  WIN32 ""
use ${GiGaVisUI_use} 
use VisSvc              v5r1      Vis 
use ClhepTools          v2r1      Tools

# Simulation specialized packages
use GaussKine           v1r7      Sim
use GaussAlgs		v3r1      Sim
use GaussTools          v13r2     Sim
use GaussRICH		v7r3	  Sim
use GaussTracker	v6r0p1	  Sim
use GaussCalo		v9r0p1	  Sim
use GaussPhysics	v5r0	  Sim
use GaussMonitor	v5r0	  Sim
use CaloMoniSim         v3r1p1    Calo
use VeloGauss           v2r2      Velo
use MuonMoniSim         v2r2      Muon

# GMDL
#use GDML                v1r240p0  Sim
#use GDMLG4Writer        v2r0      Sim

# Visualization configuration
use XmlVis              v1r0      Vis


# Allow the generation of QMTest summary 
apply_pattern QMTestSummarize
