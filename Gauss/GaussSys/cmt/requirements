package GaussSys
version v39r1

branches cmt doc

#==================================================================
# Gauss packages
#==================================================================

# Gauss application: the application is not built on Win32
macro Gauss_use         "Gauss        v39r1 Sim"  WIN32 ""
use ${Gauss_use} 

# NEEDED FOR MC10
use GaudiConf           v13r8

# Generator packages: Basic configuration
macro GENSER_use        "GENSER       v8r0    Gen"  WIN32 ""
macro EvtGen_use        "EvtGen       v11r10  Gen"  WIN32 ""
macro EvtGenExtras_use  "EvtGenExtras v2r5p1  Gen"  WIN32 ""
macro Generators_use    "Generators   v10r9   Gen"  WIN32 ""
macro LbPythia_use      "LbPythia     v9r0p1  Gen"  WIN32 ""
use ${GENSER_use}
use ${EvtGen_use}
use ${EvtGenExtras_use}
use ${Generators_use}
use ${LbPythia_use}

# Generators packages: extensions to Pythia6
macro LbAlpGen_use      "LbAlpGen     v1r0    Gen"  WIN32 ""
macro BcVegPy_use       "BcVegPy      v2r1    Gen"  WIN32 ""
macro LbBcVegPy_use     "LbBcVegPy    v3r1    Gen"  WIN32 ""
macro LbHidValley_use   "LbHidValley  v2r3    Gen"  WIN32 ""
#use ${LbAlpGen_use}
use ${BcVegPy_use}
use ${LbBcVegPy_use}
use ${LbHidValley_use}

# Generators packages: alternative to Pythia6 for pp collisions
macro LbHerwig_use      "LbHerwig     v6r0p1  Gen"  WIN32 ""
macro LbPythia8_use     "LbPythia8    v2r1p1  Gen"  WIN32 ""
use ${LbHerwig_use}
use ${LbPythia8_use}

# Generators packages: alternative to Pythia6 for p-A collisions
macro LbHijing_use      "LbHijing     v5r1    Gen"  WIN32 ""
use ${LbHijing_use}

# Generators packages: LHCb sampling results for Machine Induced Background
use LbMIB               v4r1      Gen

# Generators packages: LHCb particle gun generator: temporarly off on Win32
macro LbPGuns_use       "LbPGuns v1r2    Gen"  WIN32 ""
use ${LbPGuns_use}

# Simulation basic packages
use GiGa        	v19r8	  Sim     
use GiGaCnv     	v18r7	  Sim
use SimSvc              v5r2      Sim
macro GiGaVisUI_use     "GiGaVisUI    v5r1    Sim"  WIN32 ""
use ${GiGaVisUI_use} 
use VisSvc              v5r5      Vis 
use ClhepTools          v2r1p1    Tools

# Simulation specialized packages
use GaussKine           v4r0      Sim
use GaussAlgs		v3r4      Sim
use GaussTools          v15r4     Sim
use GaussRICH		v11r2     Sim
use GaussTracker	v6r2p1	  Sim
use GaussCalo		v9r3	  Sim
use GaussPhysics	v6r5	  Sim
use GaussUMC            v1r0      Sim

# Monitoring packages
use GaussMonitor	v6r6      Sim
use CaloMoniSim         v4r0      Calo
use VeloGauss           v2r4      Velo
use MuonMoniSim         v2r5p1    Muon
use BcmMoniSim          v1r2      Bcm
use BlsMoniSim          v1r1      Bls

# GMDL
#use GDML                v1r240p0  Sim
#use GDMLG4Writer        v2r0      Sim

# Visualization configuration
use XmlVis              v1r12      Vis

# Test packages for new releases or installations on new platforms
use GenTests            v1r4      Gen
use SimChecks           v1r0      Sim

# Allow to run the QMTests  
apply_pattern QMTest

# Declare this as a container package
apply_pattern container_package

# Allow the generation of QMTest summary 
apply_pattern QMTestSummarize

#====================================================================

