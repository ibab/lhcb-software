      function dijet_pt2max_regular()
      real * 8 dijet_pt2max_regular
      write(*,*) ' dummy dijet_pt2max_regular function; replace with'
      write(*,*) ' your own process-dependent one, to be put in the'
      write(*,*) ' process-specific directory (e.g. /W, /Z, /VBF_H...)'
      write(*,*) ' The Makefile will automatically compile the version'
      write(*,*) ' in the process-specific directory'
      call exit(1)
      end
