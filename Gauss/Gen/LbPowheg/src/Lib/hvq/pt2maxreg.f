      function hvq_pt2max_regular()
      real * 8 hvq_pt2max_regular
      write(*,*) ' dummy hvq_pt2max_regular function; replace with'
      write(*,*) ' your own hvq_process-dependent one, to be put in the'
      write(*,*) ' hvq_process-specific directory (e.g. /W, /Z, /VBF_H...)'
      write(*,*) ' The Makefile will automatically compile the version'
      write(*,*) ' in the hvq_process-specific directory'
      call exit(1)
      end
