This needs a reasonably recent root version. Will work on lxplus. Runs
on my mac. Probably won't work with windows, but might.

To compile type 

   make

To run:

./pgStats < pgStats.txt >& out &

find output in out.
