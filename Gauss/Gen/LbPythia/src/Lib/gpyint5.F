      INTEGER*4 FUNCTION PYINT5_ADDRESS() 
      IMPLICIT NONE

      COMMON/PYINT5/NGENPD,NGEN(0:500,3),XSEC(0:500,3)
      INTEGER NGENPD, NGEN
      DOUBLE PRECISION XSEC
      EXTERNAL PYDATA ! All that is required to force loading of BLOCK DATA
      INTEGER*4 GETADDR

      PYINT5_ADDRESS = GETADDR(NGENPD)
      RETURN
      END
