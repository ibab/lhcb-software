C
C
      SUBROUTINE ARAN9(QRAN,NDIM)
      IMPLICIT REAL*8(A-H,O-Z)
      DOUBLE PRECISION QRAN(10)
      COMMON/SEEDVAX/NUM1,NUM1PD
      DO 1 I=1,NDIM
    1 QRAN(I)=hijing_RAN(NUM1)
      RETURN
      END
